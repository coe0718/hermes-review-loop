# hermes-review-loop
Unattended multi-agent PR review loop for Hermes: one agent writes, a different agent reviews, verdict-counted budget, stall watchdog, disk cleanup. Disclosure — writes webhook routes and a cron job, calls the GitHub API with your own repo-scope tokens.
